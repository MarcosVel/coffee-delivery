import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    width: "100%",
    zIndex: 1,
  },
  canvas: {
    height: 220,
    width: 152,
    zIndex: 2,
  },
});
